{{-- 
    Hreflang tags for bilingual support (English primary, Polish alternate)
    This helps search engines understand language targeting for Polish-speaking customers in Chicago area
--}}
<link rel="alternate" hreflang="en" href="{{ url()->current() }}" />
<link rel="alternate" hreflang="pl" href="{{ url()->current() }}" />
<link rel="alternate" hreflang="x-default" href="{{ url()->current() }}" />
